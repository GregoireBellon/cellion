export enum ViewMode {
  DEFAULT = "DEFAULT",
  BY_ROOM = "BY_ROOM",
}

export enum ColorMode {
  BY_PART = "BY_PART",
  BY_COURSE = "BY_COURSE",
  BY_TEACHER = "BY_TEACHER",
  BY_ROOM = "BY_ROOM",
}
